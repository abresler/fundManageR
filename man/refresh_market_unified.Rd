% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh_market_unified.R
\name{refresh_market_unified}
\alias{refresh_market_unified}
\title{Refresh the unified securities arrow lake}
\usage{
refresh_market_unified(
  asset_classes = c("equity", "bond", "futures", "crypto", "coin", "forex", "cfd"),
  max_rows_per_class = 500000L,
  lake_root = "~/Desktop/data/securities",
  enforce_floor = TRUE,
  return_message = TRUE
)
}
\arguments{
\item{asset_classes}{passed to \code{get_unified_securities}.}

\item{max_rows_per_class}{passed to \code{get_unified_securities}.}

\item{lake_root}{base directory (default \code{~/Desktop/data/securities}).}

\item{enforce_floor}{if \code{TRUE} (default), abort when new count < 80\% prior.}

\item{return_message}{pass-through message flag.}
}
\value{
invisibly, a list with fields \code{n_rows}, \code{path},
  \code{bytes}, \code{datetime_written}, \code{per_class} (tibble).
}
\description{
Pulls a fresh snapshot via \code{\link{get_unified_securities}} and
writes a single zstd-9 parquet file in overwrite mode to the
\code{securities/market_unified/snapshot=current/} hive partition.
Intended for 2x/day weekday cron or ad-hoc invocation.
}
\details{
Includes a row-count floor guard: if the new snapshot is <80\% of the
prior row count, the write is refused and an error is raised. Override
with \code{enforce_floor = FALSE}.
}
